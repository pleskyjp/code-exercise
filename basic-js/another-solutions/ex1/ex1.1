// 1.1 sum of even elements of array
let input = [1, 3, 6, 0, 5, -2, 9, 11, 16];

let sumOfEvenNumbers = 0;
for (const element of input) {
  // zbytek po celociselnem deleni je roven nule = cislo je sude
  if (element % 2 === 0) sumOfEvenNumbers += element;
}

console.log(sumOfEvenNumbers) ; // 20